import { Loader } from "@/components";

/**
 * @file loading.tsx
 * @description Default loading screen
 */
const Loading = () => {
  return <Loader className="b h-screen w-full" />;
};

export default Loading;
